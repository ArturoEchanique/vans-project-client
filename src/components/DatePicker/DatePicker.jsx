import { Container } from "react-bootstrap";
import Calendar from "react-calendar";


const DatePicker = () => {
    return (
       
            <Calendar />
        
    );
};

export default DatePicker;
